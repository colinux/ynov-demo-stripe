<h1><%= @book.title %></h1>

<p>Prix : <%= humanized_money_with_symbol(@book.price) %></p>

<%= form_tag orders_path do %>
  <%= hidden_field_tag 'book_id', @book.id %>
  <%= submit_tag 'Acheter' %>
<% end %>

<%= link_to("Retour à la liste", root_path) %>
